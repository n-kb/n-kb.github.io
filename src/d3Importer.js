import {scaleLinear, select, forceX, forceY, forceCollide, forceSimulation, forceManyBody, forceCenter, drag, event} from 'd3'

export default {
    scaleLinear: scaleLinear,
    select: select,
    forceX: forceX,
    forceY: forceY,
    forceCollide: forceCollide,
    forceSimulation: forceSimulation,
    forceManyBody: forceManyBody,
    forceCenter: forceCenter,
    drag: drag,
    event: event
}